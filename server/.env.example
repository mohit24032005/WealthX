# Server Configuration
PORT=5000
NODE_ENV=development

# Database Connection
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/wealthx?retryWrites=true&w=majority

# JWT Security
JWT_SECRET=your_super_secret_jwt_key_wealthx_2026

# Email / SMTP Configuration (for password reset OTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_google_app_password
SMTP_SECURE=false
SMTP_FROM=WealthX <no-reply@wealthx.local>

# Optional External API Keys (Leave blank to use built-in simulated mock providers)
GEMINI_API_KEY=
MARKET_DATA_API_KEY=
