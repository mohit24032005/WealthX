apiVersion: v1
kind: Service
metadata:
  name: wealthx-frontend-service
spec:
  selector:
    app: wealthx-frontend
  ports:
    - protocol: TCP
      port: 5173
      targetPort: 5173
      nodePort: 30517
  type: NodePort